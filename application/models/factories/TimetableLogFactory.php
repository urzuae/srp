<?php
/**
 * SRP
 *
 * SRP INELECTRA
 *
 * @category   lib
 * @package    lib_models
 * @copyright  Copyright (c) 2010 PCSMEXICO
 * @author     <arturo>, $LastChangedBy$
 * @version    1.0.2 SVN: $Id$
 */
/**
 * Dependences
 */
require_once "application/models/beans/TimetableLog.php";

/**
 * Clase TimetableLogFactory
 *
 * @category   lib
 * @package    lib_models
 * @subpackage lib_models_factories
 * @copyright  Copyright (c) 2010 PCSMEXICO 
 * @copyright  This File has been proudly generated by Bender (http://code.google.com/p/bender-modeler/). <chentepixtol> <zetta>
 * @author     <zetta> & <chentepixtol>
 * @version    1.0.2 SVN: $Revision$
 */
class TimetableLogFactory
{

   /**
    * Create a new TimetableLog instance
    * @param int $idTimetable
    * @param string $timestamp
    * @param int $idEmployee
    * @param int $type
    * @return TimetableLog
    */
   public static function create($idTimetable, $timestamp, $idEmployee, $type)
   {
      throw new Exception('Factory Deprecated');
      $newTimetableLog = new TimetableLog();
      $newTimetableLog
          ->setIdTimetable($idTimetable)
          ->setTimestamp($timestamp)
          ->setIdEmployee($idEmployee)
          ->setType($type)
      ;
      return $newTimetableLog;
   }
   
    /**
     * Método que construye un objeto TimetableLog y lo rellena con la información del rowset
     * @param array $fields El arreglo que devolvió el objeto Zend_Db despues del fetch
     * @return TimetableLog 
     */
    public static function createFromArray($fields)
    {
        $newTimetableLog = new TimetableLog();
        $newTimetableLog->setIdTimetableLog($fields['id_timetable_log']);
        $newTimetableLog->setIdTimetable($fields['id_timetable']);
        $newTimetableLog->setTimestamp($fields['timestamp']);
        $newTimetableLog->setIdEmployee($fields['id_employee']);
        $newTimetableLog->setType($fields['type']);
        return $newTimetableLog;
    }
   
}
