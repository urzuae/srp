<?php
/**
 * SRP
 *
 * SRP INELECTRA
 *
 * @category   lib
 * @package    lib_models
 * @copyright  Copyright (c) 2010 PCSMEXICO
 * @author     <arturo>, $LastChangedBy$
 * @version    1.0.2 SVN: $Id$
 */


require_once "lib/utils/Parser.php";

/**
 * Clase TimetableHourCollection que representa una collección de objetos TimetableHour
 *
 * @category   lib
 * @package    lib_models
 * @subpackage lib_models_collections
 * @copyright  Copyright (c) 2010 PCSMEXICO
 * @copyright  This File has been proudly generated by Bender (http://code.google.com/p/bender-modeler/). <chentepixtol> <zetta>
 * @author     zetta & chentepixtol
 * @version    1.0.2 SVN: $Revision$
 */
class TimetableHourCollection extends ArrayIterator
{

    /**
     * @var Parser
     */
    private $parser;

    /**
     * Constructor
     * @param array $array
     * @return void
     */
    public function __construct($array = array())
    {
        $this->parser = new Parser('TimetableHour');
        parent::__construct($array);
    }

    /**
     * Appends the value
     * @param TimetableHour $timetableHour
     */
    public function append($timetableHour)
    {
        parent::offsetSet($timetableHour->getIdTimetableHour(), $timetableHour);
        $this->rewind();
    }

    /**
     * Return current array entry
     * @return TimetableHour
     */
    public function current()
    {
        return parent::current();
    }

    /**
     * Return current array entry and
     * move to next entry
     * @return TimetableHour
     */
    public function read()
    {
        $timetableHour = $this->current();
        $this->next();
        return $timetableHour;
    }

    /**
     * Get the first array entry
     * if exists or null if not
     * @return TimetableHour|null
     */
    public function getOne()
    {
        if ($this->count() > 0)
        {
            $this->seek(0);
            return $this->current();
        } else
            return null;
    }

    /**
     * Contains one object with $idTimetableHour
     * @param  int $idTimetableHour
     * @return boolean
     */
    public function contains($idTimetableHour)
    {
        return parent::offsetExists($idTimetableHour);
    }

    /**
     * Remove one object with $idTimetableHour
     * @param  int $idTimetableHour
     */
    public function remove($idTimetableHour)
    {
        if( $this->contains($idTimetableHour) )
            $this->offsetUnset($idTimetableHour);
    }

    /**
     * Merge two Collections
     * @param TimetableHourCollection $timetableHourCollection
     * @return void
     */
    public function merge(TimetableHourCollection $timetableHourCollection)
    {
        $timetableHourCollection->rewind();
        while($timetableHourCollection->valid())
        {
            $timetableHour = $timetableHourCollection->read();
            if( !$this->contains( $timetableHour->getIdTimetableHour() ) )
                $this->append($timetableHour);
        }
        $timetableHourCollection->rewind();
    }

    /**
     * Diff two Collections
     * @param TimetableHourCollection $timetableHourCollection
     * @return void
     */
    public function diff(TimetableHourCollection $timetableHourCollection)
    {
        $timetableHourCollection->rewind();
        while($timetableHourCollection->valid())
        {
            $timetableHour = $timetableHourCollection->read();
            if( $this->contains( $timetableHour->getIdTimetableHour() ) )
                $this->remove($timetableHour->getIdTimetableHour());
        }
        $timetableHourCollection->rewind();
    }

    /**
     * Intersect two Collections
     * @param TimetableHourCollection $timetableHourCollection
     * @return TimetableHourCollection
     */
    public function intersect(TimetableHourCollection $timetableHourCollection)
    {
        $newtimetableHourCollection = TimetableHourCollection();
        $timetableHourCollection->rewind();
        while($timetableHourCollection->valid())
        {
            $timetableHour = $timetableHourCollection->read();
            if( $this->contains( $timetableHour->getIdTimetableHour() ) )
                $newtimetableHourCollection->append($timetableHour);
        }
        $timetableHourCollection->rewind();
        return $newtimetableHourCollection;
    }

    /**
     * Retrieve the array with primary keys
     * @return array
     */
    public function getPrimaryKeys()
    {
        return array_keys($this->getArrayCopy());
    }

    /**
     * Retrieve the TimetableHour with primary key
     * @param  int $idTimetableHour
     * @return TimetableHour
     */
    public function getByPK($idTimetableHour)
    {
        return $this->contains($idTimetableHour) ? $this[$idTimetableHour] : null;
    }

    /**
     * Transforma una collection a un array
     * @return array
     */
    public function toArray()
    {
        $array = array();
        while ($this->valid())
        {
            $timetableHour = $this->read();
            $this->parser->changeBean($timetableHour);
            $array[$timetableHour->getIdTimetableHour()] = $this->parser->toArray();
        }
        $this->rewind();
        return $array;
    }

    /**
     * Crea un array asociativo de $key => $value a partir de las constantes de un bean
     * @param string $ckey
     * @param string $cvalue
     * @return array
     */
    public function toKeyValueArray($ckey, $cvalue)
    {
        $array = array();
        while ($this->valid())
        {
            $timetableHour = $this->read();
            $this->parser->changeBean($timetableHour);
            $array += $this->parser->toKeyValueArray($ckey, $cvalue);
        }
        $this->rewind();
        return $array;
    }

    /**
     * Retrieve the parser object
     * @return Parser
     */
    public function getParser()
    {
        return $this->parser;
    }

    /**
     * Is Empty
     * @return boolean
     */
    public function isEmpty()
    {
        return $this->count() == 0;
    }


}
